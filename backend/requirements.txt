# Stable install entrypoint.
# `requirements-core.txt` is guaranteed to resolve from public package indexes.
# Install optional packages separately from `requirements-optional.txt` if needed.
-r requirements-core.txt
